<Window x:Class="DiagLauncher.Views.AddEditAppDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Application"
        Width="500"
        SizeToContent="Height"
        MaxHeight="680"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Background="Transparent">

    <Window.Resources>
        <!-- Champ texte thémé -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,14,0,5"/>
        </Style>
        <Style x:Key="StyledTextBox" TargetType="TextBox">
            <Setter Property="Background"   Value="{DynamicResource InputBackground}"/>
            <Setter Property="Foreground"   Value="{DynamicResource PrimaryText}"/>
            <Setter Property="BorderBrush"  Value="{DynamicResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"      Value="10,9"/>
            <Setter Property="FontSize"     Value="13"/>
            <Setter Property="Height"       Value="38"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush"   Value="{DynamicResource PrimaryText}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="StyledComboBox" TargetType="ComboBox">
            <Setter Property="Background"   Value="{DynamicResource InputBackground}"/>
            <Setter Property="Foreground"   Value="{DynamicResource PrimaryText}"/>
            <Setter Property="BorderBrush"  Value="{DynamicResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height"       Value="38"/>
            <Setter Property="FontSize"     Value="13"/>
            <Setter Property="Padding"      Value="10,0"/>
        </Style>
        <!-- Bouton primaire -->
        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Background"     Value="{DynamicResource AccentColor}"/>
            <Setter Property="Foreground"     Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"        Value="22,10"/>
            <Setter Property="FontSize"       Value="13"/>
            <Setter Property="FontWeight"     Value="SemiBold"/>
            <Setter Property="Cursor"         Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Bouton secondaire -->
        <Style x:Key="BtnSecondary" TargetType="Button" BasedOn="{StaticResource BtnPrimary}">
            <Setter Property="Background" Value="{DynamicResource TileBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
        </Style>
        <!-- Bouton fantôme (parcourir) -->
        <Style x:Key="BtnGhost" TargetType="Button">
            <Setter Property="Background"     Value="Transparent"/>
            <Setter Property="Foreground"     Value="{DynamicResource AccentColor}"/>
            <Setter Property="BorderBrush"    Value="{DynamicResource AccentColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"        Value="14,9"/>
            <Setter Property="FontSize"       Value="12"/>
            <Setter Property="FontWeight"     Value="SemiBold"/>
            <Setter Property="Cursor"         Value="Hand"/>
            <Setter Property="Height"         Value="38"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Ombre portée via Border externe -->
    <Border Margin="10" CornerRadius="12"
            Background="{DynamicResource DialogBackground}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.35" Direction="270"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="52"/>   <!-- Barre de titre custom -->
                <RowDefinition Height="1"/>    <!-- Séparateur -->
                <RowDefinition Height="*"/>    <!-- Contenu scrollable -->
                <RowDefinition Height="Auto"/> <!-- Footer boutons -->
            </Grid.RowDefinitions>

            <!-- ══ TITRE ══ -->
            <Border Grid.Row="0" CornerRadius="12,12,0,0"
                    Background="{DynamicResource HeaderBackground}"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Rectangle Width="3" Height="18" RadiusX="2" RadiusY="2"
                                   Fill="{DynamicResource AccentColor}" Margin="0,0,10,0"/>
                        <TextBlock x:Name="TitleText" Text="Ajouter une application"
                                   FontSize="14" FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Bouton fermer -->
                    <Button Grid.Column="2" Width="32" Height="32"
                            Click="Cancel_Click" Cursor="Hand"
                            VerticalAlignment="Center"
                            ToolTip="Fermer">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Bd" CornerRadius="6" Background="Transparent"
                                        Width="32" Height="32">
                                    <TextBlock Text="✕" FontSize="12"
                                               Foreground="{DynamicResource SecondaryText}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource DangerColor}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Séparateur -->
            <Rectangle Grid.Row="1" Fill="{DynamicResource SeparatorColor}"/>

            <!-- ══ FORMULAIRE (scrollable) ══ -->
            <ScrollViewer Grid.Row="2"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="24,4,24,0">
                <StackPanel Margin="0,0,0,16">

                    <!-- Nom -->
                    <TextBlock Text="NOM DE L'APPLICATION *" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="NameBox" Style="{StaticResource StyledTextBox}"
                             TextChanged="NameBox_TextChanged"/>

                    <!-- Badge reconnu -->
                    <Border x:Name="RecognizedBadge" Visibility="Collapsed"
                            Margin="0,6,0,0" Padding="10,5"
                            CornerRadius="4" HorizontalAlignment="Left">
                        <Border.Background>
                            <SolidColorBrush Color="#2EA44F" Opacity="0.15"/>
                        </Border.Background>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✓ " Foreground="#2EA44F" FontSize="11" FontWeight="Bold"/>
                            <TextBlock x:Name="RecognizedText" FontSize="11" Foreground="#2EA44F"/>
                        </StackPanel>
                    </Border>

                    <!-- Chemin exécutable -->
                    <TextBlock Text="CHEMIN DE L'EXÉCUTABLE *" Style="{StaticResource FieldLabel}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="PathBox" Style="{StaticResource StyledTextBox}"
                                 IsReadOnly="True" Grid.Column="0"/>
                        <Button Grid.Column="1" Content="Parcourir…"
                                Style="{StaticResource BtnGhost}"
                                Margin="8,0,0,0"
                                Click="BrowseExe_Click"/>
                    </Grid>

                    <!-- Arguments -->
                    <TextBlock Text="ARGUMENTS DE LANCEMENT  (optionnel)" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="ArgumentsBox" Style="{StaticResource StyledTextBox}"/>

                    <!-- Catégorie -->
                    <TextBlock Text="CATÉGORIE" Style="{StaticResource FieldLabel}"/>
                    <ComboBox x:Name="CategoryBox" Style="{StaticResource StyledComboBox}"/>

                    <!-- Couleur -->
                    <TextBlock Text="COULEUR DE LA TUILE" Style="{StaticResource FieldLabel}"/>
                    <WrapPanel x:Name="ColorPanel" Margin="0,4,0,0"/>

                    <!-- Aperçu tuile -->
                    <TextBlock Text="APERÇU" Style="{StaticResource FieldLabel}"/>
                    <Border Width="140" Height="140" HorizontalAlignment="Left"
                            Background="{DynamicResource TileBackground}"
                            BorderBrush="{DynamicResource TileBorder}"
                            BorderThickness="1" CornerRadius="12" Margin="0,4,0,0">
                        <Grid ClipToBounds="True">
                            <Rectangle x:Name="PreviewAccent" Height="4" VerticalAlignment="Top"/>
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12">
                                <Border Width="52" Height="52" HorizontalAlignment="Center" Margin="0,0,0,10">
                                    <Grid>
                                        <Ellipse x:Name="PreviewEllipse" Opacity="0.15"/>
                                        <TextBlock x:Name="PreviewLetter"
                                                   FontSize="22" FontWeight="Bold"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        <Image x:Name="PreviewIcon"
                                               Width="38" Height="38"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Grid>
                                </Border>
                                <TextBlock x:Name="PreviewName"
                                           Text="Mon App"
                                           FontSize="11" FontWeight="SemiBold"
                                           Foreground="{DynamicResource TileNameText}"
                                           TextAlignment="Center" TextWrapping="Wrap"
                                           MaxWidth="110" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="PreviewCategory"
                                           FontSize="10"
                                           Foreground="{DynamicResource SecondaryText}"
                                           TextAlignment="Center" HorizontalAlignment="Center"
                                           Margin="0,3,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <!-- ══ FOOTER ══ -->
            <Border Grid.Row="3"
                    BorderBrush="{DynamicResource SeparatorColor}" BorderThickness="0,1,0,0"
                    CornerRadius="0,0,12,12"
                    Background="{DynamicResource HeaderBackground}"
                    Padding="24,14">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Annuler" Style="{StaticResource BtnSecondary}"
                            Margin="0,0,10,0" Click="Cancel_Click"/>
                    <Button Content="Enregistrer" Style="{StaticResource BtnPrimary}"
                            Click="Save_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
